@model IEnumerable<MVCMovie.Models.MatHang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenMatHang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenMatHang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDMatHang">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDMatHang">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDMatHang">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
